<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.group.board.mapper.BoardMapper">

    <select id="selectRestaurantList" resultType="restaurantDto">
        select
        id,
        name,
        area,
        menu,
        memo
        from restaurant_board
        order by id desc
    </select>

    <insert id="insertRestaurant" parameterType="restaurantDto">
    insert into restaurant_board (
    name,
    area,
    menu,
    memo
    ) values (
    #{name},
    #{area},
    #{menu},
    #{memo}
    )
    </insert>

</mapper>